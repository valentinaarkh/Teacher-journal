import { HeadingDecorationPipe } from './heading-decoration.pipe';

describe('HeadingDecorationPipe', () => {
  it('create an instance', () => {
    const pipe = new HeadingDecorationPipe();
    expect(pipe).toBeTruthy();
  });
});
